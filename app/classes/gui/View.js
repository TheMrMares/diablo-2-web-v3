export default class View {
    constructor({buttons, inputs, images, visible}){
        
        this.buttons = buttons;
        this.inputs = inputs;
        this.images = images;

        this.visible = visible;
    }
}